# 矢比べ(Yakurabe) 機能要件書(MVP)

## 1. プロジェクト概要

ダーツの「セッティング」(バレル・シャフト・フライト・チップの組み合わせ)を、ユーザー同士で共有・閲覧できるWebアプリ。

## 2. 技術スタック

| 分類 | 技術 |
|---|---|
| フレームワーク | Next.js (App Router, API Routes) |
| UI | React, Tailwind CSS, shadcn/ui |
| 状態管理(クライアント) | Zustand |
| フォーム | React Hook Form + Zod |
| データフェッチ | TanStack Query (React Query) |
| DB | MySQL |
| ORM | Prisma (v7以降。接続設定は`prisma.config.ts`で管理) |
| 認証 | メール+パスワードのみ(NextAuth.js Credentials Provider) |
| アーキテクチャ | レイヤードアーキテクチャ(Controller / Service / Repository) |
| 開発環境 | Docker Compose(Next.js・MySQLともにコンテナ) |

## 3. 画面構成(MVP)

| # | 画面 | 概要 |
|---|---|---|
| 1 | トップページ | サービス紹介、人気セッティング表示 |
| 2 | セッティング一覧 | キーワード検索のみ(詳細な絞り込みは無し) |
| 3 | セッティング詳細 | 使用商品(バレル/シャフト/フライト/チップ)をクリックするとモーダル/アコーディオンでスペック・購入ボタンを表示 |
| 4 | セッティング登録・編集 | 4項目を検索付きセレクトで選択、重量・価格は自動計算、画像1枚。削除機能も含む |
| 5 | マイページ | 自分の投稿一覧、お気に入り一覧、プロフィール編集 |
| 6 | ログイン/新規登録 | メール+パスワードのみ |

**独立した商品一覧ページ・商品詳細ページは無い。** 商品情報は必ずセッティング詳細画面経由(モーダル/アコーディオン)でのみ表示する。

## 4. 主要機能の詳細仕様

### 4.1 セッティング一覧・検索
- キーワード検索のみ(バレル重量・価格帯・プレイスタイル等の詳細な絞り込みは実装しない)

### 4.2 セッティング登録・編集・削除
- バレル/シャフト/フライト/チップの4項目を、検索付きセレクト(shadcnのCommand + Popoverによるコンボボックス)で選択する
  - 単純な`<select>`(プルダウン)は使わない
  - 初期表示(未入力時)は候補を出さず空にする
  - 候補が0件の場合は「該当する商品が見つかりません」とだけ表示し、商品追加リクエスト等の代替手段は用意しない(商品は運営がマスタデータとして管理するため、ユーザーは追加できない)
- シャフト一体型フライト(`Flight.includesShaft = true`)を選んだ場合、シャフト欄は自動的に空(null)にし、選択不可にする
- 合計重量は**バレルの重量のみ**を使用する(ダーツ業界の慣習として「重さ」はバレル重量を指すため)
- 合計価格はバレル+シャフト+フライト+チップの合計。シャフトがnull(一体型フライト選択時)の場合はシャフト分を加算しない
- 画像は1枚必須(複数枚アップロードは非対応)
- 特徴・使用感(`Setting.description`)は必須項目
- アピールポイント(安定性重視/コスパ重視/軽量/飛び重視/ストレート/バランス重視)を単一選択できる(必須項目、プルダウンで選ぶ。自由記述は不可)
- おすすめレベル(初心者向け〜上級者向け)を単一選択できる(任意項目)
- 登録済みセッティングの編集・削除が可能

### 4.3 セッティング詳細・商品情報表示
- 使用商品(バレル/シャフト/フライト/チップ)をクリックすると、同一画面内のモーダル/アコーディオンでスペック・購入ボタン(`hiveUrl`)を表示する
- 商品詳細の独立ページ(`/products/[id]`等)は作らない
- 「この商品を使った他のセッティング一覧」機能は無い(商品ページが無いため)

### 4.4 お気に入り
- セッティングに対してお気に入りを付与・解除できる
- `Setting.favoriteCount`(非正規化カウント)は、お気に入りの作成/削除と同時に`prisma.$transaction`で更新し、整合性を保つ

### 4.5 マイページ
- 自分が投稿したセッティング一覧
- お気に入りしたセッティング一覧
- プロフィール編集(ユーザー名、アバター画像、自己紹介、ダーツ歴、最高レーティング)
- フォロー/フォロワーの表示・機能は無い

### 4.6 認証
- メールアドレス+パスワードのみ(OAuthログインは非対応)
- パスワードリセット機能は無し(メール送信基盤が必要になるため後回し)

### 4.7 商品マスタデータ(Barrel / Shaft / Flight / Tip)
- 一般ユーザーは商品を登録できない。運営がSeedスクリプトまたはPrisma Studioで管理する
- 各商品の主要スペック項目は**登録必須**(NOT NULL)とし、詳細モーダルでスペックが空表示になることを防ぐ
  - Barrel: メーカー、重量、全長、最大径、素材
  - Shaft: メーカー、シャフト長、シャフト形状、素材
  - Flight: メーカー、フライト形状
  - Tip: メーカー、チップ種別
  - 任意項目のまま: 購入リンク(hiveUrl)。商品説明文(description)は廃止。画像(imageUrl)は登録必須に変更

## 5. データモデル概要

`User` / `Barrel` / `Shaft` / `Flight` / `Tip` / `Setting` / `Favorite` の7テーブル構成。テーブル名・モデル名はすべて単数形で統一する。詳細は`schema.prisma`を参照。

- `Setting.shaftId`はnullable(シャフト一体型フライト選択時)
- `Setting.totalWeight`はバレル重量のみを保存
- `Setting.totalPrice`は4パーツの合計(シャフトnull時は除く)
- `Setting.favoriteCount`は非正規化カウント
- `Setting.recommendedLevel`はEnum(`RecommendedLevel`)、単一選択・任意項目
- `Setting.tag`はEnum(`Tag`:安定性重視/コスパ重視/軽量/飛び重視/ストレート/バランス重視)、単一選択・必須項目

## 6. MVPスコープ外(Phase 2以降で検討)

以下はMVPには含めない。

| 機能 | 備考 |
|---|---|
| 評価・レビュー(星評価) | UI表示も含め非対応。schemaにratingAvg/ratingCountも無い |
| OAuthログイン(Google/Apple) | メール+パスワードのみ |
| フォロー/フォロワー機能 | 全面非対応 |
| 複数画像アップロード | 商品・セッティングとも画像1枚のみ |
| 詳細な絞り込み検索 | キーワード検索のみ |
| 商品の独立した一覧・詳細ページ | セッティング詳細内のモーダル/アコーディオンで代替 |
| 「この商品を使った他のセッティング一覧」 | 商品詳細ページが無いため遷移先も無し |
| パスワードリセット機能 | メール送信基盤の整備が必要なため保留 |
| 商品登録UI(管理者用) | Seedスクリプト・Prisma Studioで代替 |

## 7. 未決定・今後の検討事項

- Seedスクリプトの初期投入データ内容
- 認証(NextAuth.js Credentials Provider)の実装詳細
