import { describe, it, expect } from "vitest";

// CI(GitHub Actions)がテストを実行できることを確認するためのプレースホルダー。
// 実装が進んだら、ここにService層やユーティリティ関数のテストを追加していく。
describe("sample", () => {
  it("テスト実行の疎通確認", () => {
    expect(1 + 1).toBe(2);
  });
});
